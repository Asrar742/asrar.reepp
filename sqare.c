#include<stdio.h>

float squarearea(float side);
float circlerad(float rad);
float rectangle(float a,float b);
int main(){
    float a = 6;
    float b = 7;

return 0;
}

float squarearea( float side){
    return side * side;
} 
float circlearea(float rad){
    return 3.14 * rad * rad;
}
float rectanglearea(float a ,float b)
{
    return a*b;
}
